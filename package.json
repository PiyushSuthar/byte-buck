{
  "name": "bytes-buck",
  "version": "0.0.1",
  "description": "All the tools you need to play with bytes.",
  "main": "dist/index.js",
  "repository": "https://github.com/piyushsuthar/bytes-buck",
  "author": "Piyush Suthar",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^15.0.1",
    "rimraf": "^3.0.2",
    "serve": "^11.3.2",
    "typedoc": "^0.20.36",
    "typescript": "^4.2.4"
  },
  "types": "dist/index.d.ts",
  "scripts": {
    "build": " rimraf dist && tsc",
    "prepare": "rimraf dist && tsc",
    "doc": "rimraf docs && typedoc",
    "serve": "serve docs"
  }
}
